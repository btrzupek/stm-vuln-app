# Auto-generated by recall-portal for btrzupek/stm-vuln-app.
# Runs on every push to main and ships a delta payload to recall.
# RECALL_INGEST_TOKEN must be set in this repo's Actions secrets (the portal
# sets this for you when M1.6 lands; until then, paste the ingest token shown
# during onboarding into Settings → Secrets and variables → Actions).

name: Reindex on merge

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  reindex:
    uses: digicert/recall/.github/workflows/reusable-reindex.yml@v1
    with:
      recall_url: https://recall-dev-agitk5jfpq-uc.a.run.app
      tenant_id: btrzupek
      repo_id: stm-vuln-app
      mode: full
    secrets:
      RECALL_INGEST_TOKEN: ${{ secrets.RECALL_INGEST_TOKEN }}
